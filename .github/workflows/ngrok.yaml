name: Host SSH session.
run-name: "Host SSH session (${{ github.event.inputs.runs-on }})"
on:
  workflow_dispatch:
    inputs:
      runs-on:
        description: 'OS to run on.'
        type: choice
        options:
          # Ubuntu
          - ubuntu-latest
          - ubuntu-24.04
          - ubuntu-24.04-arm
          - ubuntu-22.04
          - ubuntu-22.04-arm
          - ubuntu-slim
          # macoOS
          - macos-15-intel
          - macos-latest
          - macos-15
          - macos-14
          # Windows
          - windows-latest
          - windows-2025
          - windows-2022
          - windows-11-arm
        default: ubuntu-latest

jobs:
  ngrok:
    environment: Ngrok
    runs-on: "${{ github.event.inputs.runs-on }}"
    steps:
      - name: Start SSH session
        uses: Jackenmen/debug-via-ssh@ec816aed287bcd02d4731451e9b07d0150031bf7
        with:
          NGROK_AUTH_TOKEN: ${{ secrets.NGROK_AUTH_TOKEN }}
          SSH_PASS: ${{ secrets.SSH_PASS }}
